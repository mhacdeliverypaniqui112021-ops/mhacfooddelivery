MHAC DELIVERY V1.5 BUILD-UP TEST
Admin-only build for testing the new Store → Category → Food → Choices/Add-ons → Publish flow.
Existing production repository should NOT be deleted or replaced until this build is tested.
