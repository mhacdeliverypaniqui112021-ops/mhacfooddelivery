MHAC DELIVERY V4.1.9 — FIX ONLY

FIXES:
- Admin and Rider chat panels reduced so Android keyboard does not cover the input/send buttons.
- Admin <-> Rider chat now watches assigned active orders in the background.
- Incoming Admin -> Rider message automatically opens the Rider chat panel and plays the existing alert ringtone.
- Incoming Rider -> Admin message automatically opens the Admin chat panel and plays the existing alert ringtone.
- Chat ringtone uses the same existing MHAC alert audio path.
- No new business features added.
- Customer app remains unchanged from V4.1.8.

NOTE:
Mobile browsers can block autoplay audio until the user has interacted with the page. The visible Enable Sound button remains hidden; the app attempts automatic sound setup and unlocks audio from the user's first normal screen touch/click.
