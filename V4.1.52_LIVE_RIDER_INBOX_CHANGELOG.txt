MHAC DELIVERY V4.1.52 — DEDICATED LIVE RIDER INBOX

ROOT FIX:
- Added riderInbox/{riderUid}/orders/{orderId} as the PRIMARY live rider delivery channel.
- Admin writes a complete order snapshot to the assigned rider's inbox.
- Rider listens directly to that inbox; it does not depend on a Firestore query over /orders.
- Legacy riderAssignments and canonical assignedRiderUid/riderUid queries remain as compatibility fallbacks.
- Rider status changes are mirrored back to the rider inbox.

FIRESTORE RULES:
- Added riderInbox rules: rider can read/update only their own inbox; admin can create/write.

DEPLOY:
- Upload admin.html, rider.html, customer.html and firestore.rules from this package.
- IMPORTANT: deploy firestore.rules to Firebase before testing assignment.

TEST:
1. Customer places a NEW order.
2. Admin accepts the order.
3. Admin assigns the APPROVED rider.
4. Rider app should show the assigned order immediately.
5. Test PICK UP -> ON THE WAY -> DELIVERED.
