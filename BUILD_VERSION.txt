MHAC DELIVERY — V4.0.6 MENU RECOVERY FIX

FIXES:
- Fixed Admin menu loader so an empty/stale localStorage draft can no longer hide a valid Firestore menu.
- Firestore menuDraft is preferred when it contains stores and the local backup is empty.
- Published customer menu is used as fallback when no editable draft exists.
- Published stores are copied back into the Admin draft and local backup.
- Existing Store/Menu Editor, food editing, add-ons, drinks, Original/Spicy selection, publishing and rider/order features are preserved.
- Build marker: V4.0.6 MENU RECOVERY FIX

IMPORTANT:
This patch can recover stores that still exist in Firebase. If the Firebase menu document itself was already deleted/overwritten before this build, the old data cannot be reconstructed from this ZIP alone.
