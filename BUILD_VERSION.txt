MHAC DELIVERY — V4.0.4 STORE PERSISTENCE TEST BUILD

Fix in this test:
- Admin food/store/category draft saves now write immediately instead of using a delayed 250ms save.
- Local device backup is created for the Admin draft.
- On refresh, if the Firestore draft is missing, the app recovers the last local Admin draft instead of showing an empty Store/Menu Manager.
- Food SAVE reports whether the Firestore draft write succeeded.
- Existing Admin/Rider sound fix and Admin cancel-order fix retained.
- Customer UI/GPS/order flow intentionally unchanged.
