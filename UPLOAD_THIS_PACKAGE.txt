MHAC DELIVERY V4.1.42 ADMIN SYNC FIX

ADMIN FIXES
- Admin menu draft writes now go immediately to Firestore instead of relying on a delayed timer.
- Local draft backup remains enabled.
- Food edits explicitly report Firestore failures instead of looking successfully synced.
- GCash save now writes to settings/payment with a timeout and explicit Firebase error.
- GCash QR is compressed smaller to avoid Firestore document-size failures.
- Included firestore.rules allows Admin writes to settings/menu, settings/menuDraft, and settings/payment.

DEPLOYMENT
- Upload admin.html, customer.html, rider.html to GitHub Pages.
- Deploy firestore.rules in Firebase Console.
- After deployment, hard-refresh Admin and test GCash Save first, then edit one food and refresh.
